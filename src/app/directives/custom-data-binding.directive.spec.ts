import { CustomDataBindingDirective } from './custom-data-binding.directive';

describe('CustomDataBindingDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomDataBindingDirective();
    expect(directive).toBeTruthy();
  });
});
